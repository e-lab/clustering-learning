this library includes all Lua/Torch/xlearn code from elab

GaborLayer: generates a layer of gabor filters

SADtest: test sum of absolute differences

clustering learning:
   1 and 2 layers networks, generation of filters, tests on CIFAR10